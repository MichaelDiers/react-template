/**
 * Creates the main element of the page.
 * @returns {JSX.Element} A new react component.
 * @type {() => JSX.Element}
 */
const Main: () => JSX.Element = (): JSX.Element => {
  return (
    <main>
      <h1>Hello World!</h1>
    </main>
  );
}

export default Main;
