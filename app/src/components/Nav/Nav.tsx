/**
 * Creates the nav element of the page.
 * @returns {JSX.Element} A new react component.
 * @type {() => JSX.Element}
 */
const Nav: () => JSX.Element = (): JSX.Element => {
  return (
    <nav></nav>
  );
}

export default Nav;
