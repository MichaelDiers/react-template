/**
 * Creates the footer element of the page.
 * @returns {JSX.Element} A new react component.
 * @type {() => JSX.Element}
 */
const Footer: () => JSX.Element = (): JSX.Element => {
  return (
    <footer></footer>
  );
}

export default Footer;
