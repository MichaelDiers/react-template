/**
 * Creates the header element of the page.
 * @returns {JSX.Element} A new react component.
 * @type {() => JSX.Element}
 */
const Header: () => JSX.Element = (): JSX.Element => {
  return (
    <header></header>
  );
}

export default Header;
